<!DOCTYPE html>
<html>
  <head>
    <title>Annonces</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ" crossorigin="anonymous">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-toggleable-md navbar-inverse bg-primary">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Annonces</a>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
          <% if session[:user_id].nil? %>
            <a class="nav-link" href="/users/login">S'identifier / S'enregistrer</a>
          <% else %>
             <%= link_to "Se déconnecter", user_path(session[:user_id]), class: "nav-link", method: :delete %>
          <% end %>
          </li>
        </ul>
        <% if !session[:admin].nil? %>
          <span class="navbar-text">
            [Vous êtes connecté en tant qu'Administrateur]
          </span>
        <% end %>
      </div>
    </nav>
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <% if !@advertisements.nil? %>
        <h1 class="display-3">Annonces disponibles</h1>
        <p class="lead">Consultez les annonces disponibles ou publiez-en une !</p>
        <% else %>
          <h1 class="display-3">Aucune annonce trouvée</h1>
          <p class="lead">Inscrivez-vous et publiez-en une !</p>
        <% end %>
        <%= link_to 'Poster une nouvelle annonce', new_advertisement_path, class: 'btn btn-primary' %>
      </div>
    </div>
    
    <div class="container-fluid">
      <div class="container">
        <% if flash[:info] %>
          <div class="alert alert-info"><%= flash[:info] %></div>
        <% end %>
      </div>
      <%= yield %>
    </div>
  
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/js/bootstrap.min.js" integrity="sha384-vBWWzlZJ8ea9aCX4pEW3rVHjgjt7zpkNpZk+02D9phzyeVkE+jo0ieGizqPLForn" crossorigin="anonymous"></script>
  </body>
</html>
