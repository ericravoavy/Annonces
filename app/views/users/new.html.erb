<div class="container">
<%= form_for :user, url: users_path do |f| %>
 
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited
        this user from being saved:
      </h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

      <form class="form-signin">
        <h2 class="form-signin-heading">Merci de vous enregistrer</h2>
        <div class="form-group">
            <%= f.label :name, "Votre Nom ou un pseudo" %>
            <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="form-group">
            <%= f.label :password, "Votre mot de passe (8 caractères minimum svp)" %>
            <%= f.password_field :password, class: "form-control" %>
        </div>
        <div class="form-group">
            <%= f.submit "S'enregistrer", class: "btn btn-lg btn-primary btn-block" %>
        </div>
      </form>

    </div> <!-- /container -->
 
<% end %>